character:
  display_name: "Me"
  dialog_color: "#6B8EFF"
  inner_dialog_color: "#6B8EFF88"
  size_cm:
    height: 170
    width: 60

defaults:
  orientation: "front"
  pose: "idle"
  expression: "neutral"
  outfit: "default"
  body: "default"

layers:
  - id: "body"
    layer: "body"
    image: "images/{plan}/{orientation}/body/{pose}_{body}_{status}.png"
    z: 0
    anchor: {x: 0, y: 0}

